# Data Grid Component (React + TypeScript + Tailwind + Vite)

A production‑ready, accessible data grid built with **React 18**, **TypeScript (strict mode)**, **Vite**, and **Tailwind CSS**.  
This project demonstrates pagination, accessibility (ARIA roles, keyboard navigation), and performance best practices.

---

## 🚀 Tech Stack
- React 18+
- TypeScript (strict mode enabled)
- Vite
- Tailwind CSS
- ESLint + Prettier
- Testing Library (React) + axe-core

---

## 📦 Installation

Clone the repository and install dependencies:

```bash
git clone https://github.com/your-username/my-datagrid-project.git
cd my-datagrid-project
npm install

▶️ Running the Project
Start the development server:
npm run dev


Folder Structure
src/
├── components/
│   └── DataGrid.tsx       # Reusable data grid component
├── App.tsx                # Demo entry point
├── index.css              # Tailwind base styles
├── main.tsx               # Vite entry
└── tests/
    └── DataGrid.test.tsx  # Accessibility + interaction tests
